<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.textbook.mapper.FavoriteMapper">

    <select id="selectByUserId" resultType="com.campus.textbook.entity.Favorite">
        SELECT * FROM t_favorite WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM t_favorite WHERE user_id = #{userId}
    </select>

    <select id="selectByUserAndBook" resultType="com.campus.textbook.entity.Favorite">
        SELECT * FROM t_favorite WHERE user_id = #{userId} AND textbook_id = #{bookId} LIMIT 1
    </select>

</mapper>
