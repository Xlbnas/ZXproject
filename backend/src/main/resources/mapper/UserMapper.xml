<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.textbook.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.campus.textbook.entity.User">
        SELECT * FROM t_user WHERE username = #{username} LIMIT 1
    </select>

    <select id="selectUserList" resultType="com.campus.textbook.entity.User">
        SELECT * FROM t_user
        <where>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%',#{keyword},'%')
                OR real_name LIKE CONCAT('%',#{keyword},'%')
                OR phone LIKE CONCAT('%',#{keyword},'%')
                OR email LIKE CONCAT('%',#{keyword},'%'))
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countUserList" resultType="int">
        SELECT COUNT(*) FROM t_user
        <where>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%',#{keyword},'%')
                OR real_name LIKE CONCAT('%',#{keyword},'%')
                OR phone LIKE CONCAT('%',#{keyword},'%')
                OR email LIKE CONCAT('%',#{keyword},'%'))
            </if>
        </where>
    </select>

</mapper>
